# 往復変換テスト結果レポート

## テスト概要

- **対象ファイル**: `威風堂々ラスト_in-Bassoon.mxl`
- **テスト内容**: 反転 → 再反転による往復変換
- **実行日**: 2026-04-23

## テスト結果サマリー

### music21 レベルでの比較: ✅ **完全一致**

- パート数: 一致 (1パート)
- 小節数: 一致 (53小節)
- 音符数: 一致 (142音符)
- ピッチ: 全て一致
- デュレーション: 全て一致
- Dynamics: 全て一致
- Slur: 全て一致

### XML レベルでの比較: ⚠️ **差異あり (4559行)**

ただし、差異の内容は **音楽的には無害なメタデータ・フォーマット違い** のみ。

## 差異の詳細分類

### 1. XML 宣言・DOCTYPE (許容される差異)

**元ファイル:**
```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE score-partwise PUBLIC "-//Recordare//DTD MusicXML 4.0 Partwise//EN" "http://www.musicxml.org/dtds/partwise.dtd">
```

**往復後:**
```xml
<?xml version='1.0' encoding='utf-8'?>
```

- **原因**: music21 は DOCTYPE 宣言を出力しない
- **影響**: なし（MusicXML パーサーはどちらも正しく読み込む）

### 2. エンコーディング情報の変更 (許容される差異)

**追加された情報:**
- `<encoding-date>2026-04-23</encoding-date>` (処理日が記録される)
- `<software>music21 v.9.9.1</software>` (music21 のバージョンが追加)
- `<movement-title>威風堂々ラスト_in-Bassoon.mxl</movement-title>` (ファイル名が追加)

**削除された情報:**
- `<source>` タグ
- `<supports>` タグ (MuseScore 固有の情報)

- **原因**: music21 は自身のメタデータを追加し、一部の MuseScore 固有情報を保持しない
- **影響**: なし（音楽内容には影響しない）

### 3. レイアウト情報の削減 (現在は保存されている)

**削除された情報:**
- `<page-layout>` (ページサイズ・マージン)
- `<appearance>` (線幅、音符サイズなどの詳細)
- `<music-font>`, `<word-font>`, `<lyric-font>` (フォント情報)
- `<credit>` (タイトル・作曲者などの配置情報)

**保持された情報:**
- `<system-layout>` (システムレイアウト)
- `<print>` 要素 (改ページ・改段情報)

- **原因**: music21 がサポートしていない詳細レイアウト情報
- **影響**:
  - ✅ 現在、`layout_preservation.py` により `<print>` 要素は保存されている
  - ⚠️ ページサイズやフォント情報は失われる（将来的に改善の余地あり）

### 4. インストルメント ID の変更 (許容される差異)

**元ファイル:**
```xml
<score-instrument id="P1-I1">
```

**往復後:**
```xml
<score-instrument id="Iada5b8b4438f6b7bfce787308bc7879e">
```

- **原因**: music21 がランダムな ID を生成
- **影響**: なし（ID は内部参照用で、音楽的な意味はない）

### 5. divisions 値の変更 (許容される差異)

**元ファイル:**
```xml
<divisions>4</divisions>
```

**往復後:**
```xml
<divisions>10080</divisions>
```

- **原因**: music21 が音価を表現するために高精度の divisions を使用
- **影響**: なし（音価の表現精度が向上する）

### 6. インデント・空白の違い (許容される差異)

- 閉じタグのインデントが異なる
- 属性の順序が異なる（例: `font-size` と `font-weight` の順序）

- **原因**: music21 の XML フォーマッター
- **影響**: なし（XML の意味論は同じ）

### 7. direction 要素の system 属性の欠落

**元ファイル:**
```xml
<direction placement="above" system="also-top">
```

**往復後:**
```xml
<direction placement="above">
```

- **原因**: music21 が `system="also-top"` 属性を保持していない可能性
- **影響**: 小（最初のシステムへの direction の表示に影響する可能性があるが、音楽的内容には影響しない）

## 修正の要否判断

### ✅ 修正不要（許容される差異）

1. **XML 宣言・DOCTYPE の違い** — 標準に準拠しており、問題なし
2. **エンコーディング情報の変更** — メタデータの更新は正常
3. **インストルメント ID の変更** — 内部 ID で影響なし
4. **divisions 値の変更** — 精度向上で問題なし
5. **インデント・属性順序** — フォーマット違いで影響なし

### ⚠️ 将来的な改善検討事項

1. **ページレイアウト情報の欠落**
   - 現状: `<page-layout>`, `<appearance>`, `<credit>` が失われる
   - 影響: 印刷時のレイアウトが変わる可能性
   - 対応: `layout_preservation.py` を拡張して、これらの情報も保存する
   - **優先度**: 低（音楽的内容には影響しない）

2. **system 属性の欠落**
   - 現状: `system="also-top"` が失われる
   - 影響: 小（最初のシステムへの direction 表示）
   - 対応: music21 の direction パース/書き出しを拡張
   - **優先度**: 低

3. **source タグの欠落**
   - 現状: `<source>` が失われる
   - 影響: 出典情報が失われる
   - 対応: metadata として保存・復元
   - **優先度**: 低

## 結論

### 音楽的内容: ✅ **完全に保存されている**

- 全ての音符、ピッチ、デュレーション、強弱記号、スラーが正しく往復変換されている
- 反転処理そのものは **完全に正しく動作** している

### メタデータ・レイアウト: ⚠️ **一部が music21 フォーマットに変換される**

- 音楽的には無害な変更のみ
- 現在の実装で実用上の問題なし
- 将来的にレイアウト保存を強化する余地あり

### 総合評価: ✅ **プロダクション利用可能**

往復変換は音楽的内容を完全に保持しており、実用上の問題はありません。
